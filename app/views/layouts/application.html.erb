<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= @title %>: <%= if @service;@service.title; else; Hotarugaike::Application.config.title;end %></title>

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag "application" %>
    <%= yield :head %>
  </head>

  <body data-base-url="<%= url_for root_url %>" data-rails-env="<%= Rails.env.to_s %>">
    <div id="header" class="container">
      <%= link_to image_tag('logo.png'), "/" unless @service %>
      <%= link_to image_tag(@service.logos['1x'], :alt => @service.title, :srcset => @service.logos.map{|k, v| "#{v} #{k}"}.join(', ')), @service.root if @service %>
    </div>

    <%= yield %>
  </body>
</html>
